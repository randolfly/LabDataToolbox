@inherits LayoutComponentBase
<MApp>
    <MNavigationDrawer App Permanent Width="80">
        <div class="d-flex flex-column" style="height: inherit">
            <MList Nav Routable Dense>
                <MListItem Href="/" ActiveClass="primary--text">
                    <MListItemContent >
                        <MListItemIcon Class="d-flex justify-center">
                            <MIcon Icon="@("mdi-home")"/>
                        </MListItemIcon>
                        <MListItemSubtitle Class="d-flex justify-center">Home</MListItemSubtitle>
                    </MListItemContent>
                </MListItem>
                <MListItem Href="/AdsDataLog" ActiveClass="primary--text">
                    <MListItemContent >
                        <MListItemIcon Class="d-flex justify-center">
                            <MIcon Icon="@("mdi-database")"/>
                        </MListItemIcon>
                        <MListItemSubtitle Class="d-flex justify-center">Log</MListItemSubtitle>
                    </MListItemContent>
                </MListItem>
            </MList>
            <MList Nav Routable Dense Class="mt-auto">
                <MListItem Href="/setting" ActiveClass="primary--text">
                    <MListItemContent>
                        <MListItemIcon  Class="d-flex justify-center">
                            <MIcon Icon="@("mdi-cog")"/>
                        </MListItemIcon>
                        <MListItemTitle  Class="d-flex justify-center">设置</MListItemTitle>
                    </MListItemContent>
                </MListItem>
            </MList>
        </div>

    </MNavigationDrawer>
    <MMain>
        <MContainer Fluid Style="height:100%">
            <MErrorHandler>
                @Body
            </MErrorHandler>
        </MContainer>
    </MMain>
</MApp>

@code {

}